DayPilot

Workday planning toolkit with a standalone Textual TUI for task capture.

Run the TUI capture screen
- `uv sync` (first time only, to install the console script)
- `uv run daypilot-tui`
- `uv run python -m daypilot.tui`
